# protocol/

Zoocall protocol v1: `.proto` schemas (`proto/zoocall/v1/`), normative spec (`spec/`), capability registry and test vectors.

Design: [docs/03-protocol.md](../docs/03-protocol.md). Not implemented yet.
